PWA icons placeholder. Replace with branded icons before prod.

Required sizes:

- `icon-192.png` — 192x192, `purpose: any`
- `icon-512.png` — 512x512, `purpose: any`
- `icon-maskable.png` — 512x512, `purpose: maskable` (must include safe-zone padding; ~10% inset on every side)

Referenced by `src/app/(marketplace)/manifest.ts`.
